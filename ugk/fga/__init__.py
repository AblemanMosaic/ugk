"""ugk.fga — Fractal Governance Architecture surface layer (additive substrate).

SB-3a-core: makes UGK's already-implicit FGA decision-surface / trace-vector
structure FIRST-CLASS and ENUMERABLE over the existing committed receipt axes
(h_s/h_c/h_m/h_j, bound by h_r and h_body). Reads receipts; commits nothing new;
moves no law, schema, or legend. The candidate capability-evidence surface (D_cap,
fed by CGP dispatcher ledgers) is DESIGN-ONLY and is deliberately ABSENT from the
committed surface registry until its receipt commitment is separately authorized.
"""
